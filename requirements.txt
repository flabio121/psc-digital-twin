# Floors, not exact pins. Streamlit Community Cloud chooses its own Python
# version and ignores .python-version, so exact pins to 3.11-era wheels fail to
# resolve there and the app launches with the package missing. The trained
# joblib artifacts were written by scikit-learn 1.5.2 and were verified to load
# and predict identically under scikit-learn 1.9 / NumPy 2.5 / Python 3.12,
# so a floor is safe here. Unpickling emits a benign version-mismatch warning.
numpy>=2.0
pandas>=2.2
matplotlib>=3.9
scikit-learn>=1.5
streamlit>=1.50
